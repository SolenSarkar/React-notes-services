{
  "name": "react-todo-list-monorepo",
  "version": "1.0.0",
  "description": "Root package.json so Render can build & start the backend from the repo root (in case rootDir is not applied).",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "start-server": "npm start"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1"
  }
}
